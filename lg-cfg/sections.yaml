src:
  extensions: [".ts", ".json", ".html", ".css", ".js"]
  filters:
    mode: allow
    allow:
      - "/src/"
      - "/media/"
      - "/package.json"
    children:
      media:
        mode: block
        block:
          - "/stats**"
          - "/doctor**"
          - "/ui/"
      src:
        mode: block
        block:
          - "**stats**"
          - "**doctor**"
          - "**diag**"
