src:
  extensions: [".ts", ".json", ".html", ".css", ".js"]
  filters:
    mode: allow
    allow:
      - "/src/"
      - "/media/"
      - "/package.json"
    children:
      media:
        mode: block
        block:
          - "/doctor**"
          - "/control**"
      src:
        mode: block
        block:
          - "/services/ai/"
          - "**doctor**"
          - "**diag**"
          - "**control**"
